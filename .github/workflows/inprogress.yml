name: Rhoderick move to in progress

on:
  issues:
    types: [assigned]

jobs:
  Issues-to-in-progress:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Print Hello World
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "Hello, World!"
          gh --version
          gh issue list
//          echo `gh project item-list`
          
