name: GitHub Project Automation+

on:
  issues:
    types: [assigned]

jobs:
  automate-project-columns:
    runs-on: ubuntu-latest
    steps:
      - uses: alex-page/github-project-automation-plus@v0.8.2
        with:
          project: "RhoderickGalero"
          column: InProgress
          repo-token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
