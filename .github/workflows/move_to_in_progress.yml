name: Move Issue to In Progress
on:
  issues:
    types:
      - assigned

jobs:
  move_to_in_progress:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
                      
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install Dependencies
        run: npm install axios @octokit/rest

      - name: Move Issue to In Progress
        run: node move-to-in-progress.js
        env:
          PERSONAL_ACCESS_TOKEN: ${{ secrets.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 }}
          GITHUB_EVENT: ${{ toJson(github.event) }} # Pass the GitHub event payload
